name: Director IR Contract

on:
  push:
    branches: [main]
    paths:
      - 'prototype/**'
      - 'tests/**'
      - 'benchmarks/**'
      - 'schemas/**'
      - 'examples/director-ir.sample.json'
      - '.github/workflows/director-ir-contract.yml'
  pull_request:
    paths:
      - 'prototype/**'
      - 'tests/**'
      - 'benchmarks/**'
      - 'schemas/**'
      - 'examples/director-ir.sample.json'
      - '.github/workflows/director-ir-contract.yml'

jobs:
  director-ir-contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install test dependencies
        run: python -m pip install pytest jsonschema
      - name: Run Director IR, semantic pipeline, and intelligence benchmark
        run: python -m pytest -q tests/test_director_to_motion.py tests/test_semantic_pipeline.py tests/test_director_intelligence_benchmark.py
